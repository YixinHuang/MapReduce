package mr

type Task struct {
	Num int //task number
	//task cmd :map or reduce
	//task state:idle, in-progress, or completed
	//task worker machine
	//stores the locations and sizes of the R intermediate ﬁle regions produced by the map
}
